# zapgw — chaves do contrato que JA estao em ingles e NAO mudam no passo 4.
#
# FORMATO ESTAVEL, POR CONTRATO COM OS CONSUMIDORES: uma chave por linha, sem
# ordem significativa, sem colunas. Linha vazia e linha comecando com '#' sao
# comentario. Este arquivo existe para ser lido por TESTE, nao por humano — a
# versao explicada, com arquivo:linha, struct e rota, esta em
# docs/INVENTARIO-CHAVES.md (secao "Item 4") e em docs/MIGRACAO-CONTRATO-EN.md.
#
# POR QUE ELE EXISTE: uma tabela de renomeacao que so lista renomeacoes deixa o
# leitor inferir o resto — "o que nao esta aqui e' portugues" — e essa frase
# nunca foi escrita por ninguem. Foi assim que o `media_id` entrou no mapa de um
# consumidor e o upload de midia parou. A tabela e' fonte de RENOMEACAO, nao de
# IDIOMA (frase do consumer-b, 2026-08-31).
#
# COMO USAR: se alguma destas chaves aparecer no seu mapa de traducao, o seu
# teste deve REPROVAR. Elas ja estao no idioma final.
#
# ESTABILIDADE: este arquivo nao e' reformatado. Chave sai daqui apenas se
# deixar de existir no contrato, e isso e' quebra de contrato — anunciada, nunca
# silenciosa.

media_id
latitude
longitude
id
phone_number_id
waba_id
timestamp
wa_message_id
status
template
ig_id
wa_id
templates
ok
about
address
description
email
profile_picture_url
websites
vertical
profile_picture_handle

# ---------------------------------------------------------------------------
# ACRESCENTADO EM 2026-08-31, depois de o consumer-b perguntar ao contrario:
# "para cada um dos 29, a forma inglesa JA e' emitida em algum lugar?"
#
# A pergunta achou DOIS casos reais, e nenhum dos dois aparecia cruzando as
# duas listas — so' medindo contra o codigo. As outras oito formas inglesas
# que existem no codigo (`code`, `details`, `caption`, `location`, `reaction`,
# `value`, `voice`, e `components` em internal/meta/templates.go) estao em
# DECODIFICADORES do formato CRU da Meta (internal/meta/parse.go,
# bloqueio.go): morrem por dentro, nunca chegam ao consumidor, e por isso nao
# entram nesta lista.
#
# `address` — ja estava aqui, e o motivo agora esta escrito: ele e' ingles no
#   objeto de perfil (GET/POST /v1/perfil) E e' o destino de `endereco` no
#   passo 4. Depois da virada, os dois sao `address` em objetos DIFERENTES, e
#   nenhum dos dois precisa de traducao nunca.
#
# `components` — ele existe na ENTRADA do POST /v1/messages
#   (internal/outbound/mensagem.go:576) **para ser RECUSADO**: a uniao
#   discriminada existe para tornar INEXPRESSIVEL o que a Meta rejeita, e
#   deixar `components` cru passar devolveria essa classe de erro inteira para
#   producao. Mapear `components` -> `componentes` faz esse campo-armadilha
#   deixar de ser reconhecido. Note que `componentes` (portugues) TAMBEM
#   existe, noutra rota (criacao de template) — sao dicionarios diferentes, e
#   e' por isso que a regra do consumer-b e' POR DICIONARIO, nunca global.
# ---------------------------------------------------------------------------

components
